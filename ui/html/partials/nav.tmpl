{{ define "nav" }}
<nav>
    <div>
        <a href="/">Home</a>
    </div>
    <div>
        {{ if .IsAuthenticated }}
            <a href="/account/view">Account</a>
        {{ else }}
            <a href="/signup">Sign Up</a>
            <a href="/login">Login</a>
        {{ end }}
    </div>
</nav>
{{ end }}
